# Base URL of the Git Secure-AI backend.
# Leave UNSET to run the frontend standalone on mock data (default).
# Set it to enable live mode: "Connect GitHub" starts the real OAuth flow and
# the repo list loads from the backend.
# VITE_API_URL=http://localhost:8787
